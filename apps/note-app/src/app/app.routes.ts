import { Route } from '@angular/router';
import { AppComponent } from './app.component';
import { ConfirmBoxComponent } from '@app/shared-components';

export const appRoutes: Route[] = [{ path: '', component: AppComponent }];
